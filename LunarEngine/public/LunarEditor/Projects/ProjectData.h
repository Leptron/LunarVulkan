#pragma once
#include <string>

namespace LunarEditor {
    struct UserProject {
        std::string projectName;
        std::string projectLocation;
        std::string projectDescription;
        
    };
}